import { createStore } from 'vuex'
import { alerts } from './alerts'

export default createStore({
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        alerts
    }
})
